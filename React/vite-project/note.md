---
1. React consider Tag as data
2. We can store and return tag
3. Function can return only one Tag
4. use {} inside tag to perform js operations (+, -, ... variable call, )

We can only return one tag so if we want to return multiple tags we can use div element as a wrapper and inside it we can add as may as we want...

