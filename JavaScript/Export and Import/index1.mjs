export let name = "Ram"; // export by name
export let age = 19;

let country = "Nepal"; 
export default country; // export by default

/*
    export by name -> add just prefix at the front of variable declartion and one file can have many exports by name
    export by default -> only one export by default could be there 
*/